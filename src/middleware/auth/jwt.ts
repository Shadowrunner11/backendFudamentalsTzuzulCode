/* import { Strategy, ExtractJwt } from 'passport-jwt';
import userController from '../../controllers/user.controller';

const options = {
  jwtFromRequest: ExtractJwt.fromBodyField('phpSSID'), // pa confundir a la gente
  secretOrKey: 'secret', */
/*   issuer: 'accounts.examplesoft.com',
  audience: 'yoursite.net' */
/* }; */

/* const JwtStrategy = new Strategy(options, (jwt, next)=>{
  userController.
});  */

